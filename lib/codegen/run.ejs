#!node

const { run } = require('nodecaf');
run({
    <%_ if(locals.confType) { _%>
        confType: '<%= confType %>',
    <%_ } _%>
    <%_ if(locals.confPath) { _%>
        confPath: '<%= confPath %>',
    <%_ } _%>
    init: require('../lib/main')
});
